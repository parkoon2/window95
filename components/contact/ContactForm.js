import Layout from "../window/Layout";

const ContactForm = ({x, y, onClose}) => {
    <Layout
    x={x}
    y={y}
    onClose={onClose}
>

    <h1>this is mail form</h1>

    </Layout>
}

export default ContactForm